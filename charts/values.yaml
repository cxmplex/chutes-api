teeMinimumBootVersion: ""
snpCrlOutageGraceSeconds: 0
# Comma-separated CIDRs for direct final-hop proxies that overwrite X-Resolved-IP.
# Empty is rendered explicitly and parses as no trusted proxies; it never inherits an internal CIDR.
# Trust only sanitizing final hops controlled by this deployment. Do not trust third-party proxy
# images until their header behavior is verified.
trustedProxyCidrs: ""
# Required by production API startup. These CIDRs authorize only the direct socket peers that may
# call /readyz and /_metrics; forwarded and resolved-client headers are ignored. Include the actual
# kubelet/CNI probe and monitoring peer ranges for the deployment. Empty is a fail-closed production
# placeholder; standalone development with this setting unset defaults to loopback.
operatorEndpointCidrs: ""
dbSsl: "require"

provenance:
  publicKeySecretName: guest-provenance-public-key
  publicKeySecretKey: cosign.pub

l0Publisher:
  publicKeyRegistrySecretName: l0-publisher-public-keys
  publicKeyRegistrySecretKey: keys.json

# Signing keys bundle served at GET /servers/signing-keys (rendered to JSON by
# templates/signing-keys-cm.yaml). These are PUBLIC keys + their detached PGP signatures, so they
# live in values (not a Secret) -- but the REAL base64 values belong in the env overlay, not here.
# Placeholders keep the chart renderable; the API validates every entry is a non-empty string, and
# a booting VM verifies each key against its signature, so real signatures are required too.
# version MUST stay an int (unquoted). See templates/signing-keys-cm.yaml for how to produce each.
signingKeysBundle:
  version: 1
  keys:
    cosign/chutes.pub: "PLACEHOLDER_BASE64_COSIGN_CHUTES_PUB"
    cosign/dockerhub.pub: "PLACEHOLDER_BASE64_COSIGN_DOCKERHUB_PUB"
    helm-pubkey.gpg: "PLACEHOLDER_BASE64_HELM_PUBKEY_GPG"
  signatures:
    cosign/chutes.pub: "PLACEHOLDER_BASE64_COSIGN_CHUTES_PUB_SIG"
    cosign/dockerhub.pub: "PLACEHOLDER_BASE64_COSIGN_DOCKERHUB_PUB_SIG"
    helm-pubkey.gpg: "PLACEHOLDER_BASE64_HELM_PUBKEY_GPG_SIG"

# Registry auth path selector (see api/registry/router.py): VMs attested at >= this
# version must use mTLS to pull; older VMs use legacy hotkey auth.  Set to "0.0.0" to
# force every attested VM onto mTLS — the kill switch that retires legacy auth once
# the fleet is fully migrated (coordinated with removing the guest nginx DaemonSet).
registryMtlsMinVersion: "1.4.0"

# Attestation mTLS gate (TEE_MTLS_MIN_VERSION): VMs attested at >= this must reach the
# transitional attestation endpoints (nonce, luks/confirm) via the cvm proxy, not the legacy
# api.chutes.ai path (see gate_legacy_attestation). "0.0.0" forces every attested VM onto cvm.
teeMtlsMinVersion: "1.4.0"

squad_enabled: false

account_settings:
  first_payment_bonus:
    threshold: 100.00
    bonus: 25.00
  all_accounts_free: false
  developer_deposit: 5000.00

chutes_version: "0.6.9"

# Default Job/pod retention for all CronJobs. Each cronjob inherits these unless it
# sets the same key under its own values block (e.g. logProber.ttlSecondsAfterFinished).
# Lower these to keep fewer completed/failed Job pods lingering in the namespace.
cronjobDefaults:
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 3
  ttlSecondsAfterFinished: 3600 # delete finished Jobs (and their pods) after 1h

# Container image registry prefix. All private images are stored under this path.
# Set to empty string to use fully-qualified image values as-is.
imageRegistry: "us-central1-docker.pkg.dev/chutes-443309/chutes-prod-docker-registry"

consecutive_failure_limit: 7

# NetworkPolicies for the workloads (reproduced from the live cluster). NOTE: 7 of the 8 select
# pods by `app: <name>`, but chart pods are labeled app.kubernetes.io/name -- so those policies
# currently match no pods and enforce nothing (only pg-router-np is active). Kept faithful for a
# no-op adoption; fixing the selectors is a deliberate, test-gated change (see template warnings).
networkPolicies:
  # Boolean values must be YAML booleans, not quoted strings.
  enabled: true
  # Egress CIDRs are environment-specific -- placeholders here (RFC5737 docs net), real ranges
  # set in the overlay. internalCidr = private range for redis/postgres/internal services;
  # proxyUpstreamCidr = the external upstream the claude/responses proxies are allowed to reach.
  internalCidr: "192.0.2.0/24"
  proxyUpstreamCidr: "192.0.2.4/32"
  # Optional ingress isolation for API pods. This chart accepts only pod-bound peers containing
  # BOTH non-empty namespaceSelector.matchLabels and podSelector.matchLabels maps. It rejects
  # ipBlock, matchExpressions, empty selectors, and unknown keys. Use this path for ingress
  # controllers, monitoring, and internal proxy pods.
  api:
    enabled: false
    ingressPeers: []
    # Node/CNI sources requiring CIDRs must use an externally managed policy. Set this boolean
    # acknowledgement only when that policy selects app.kubernetes.io/name=api and limits TCP/8000
    # to verified, sanitizing final hops. Quoted "true"/"false" values fail rendering.
    # A non-empty trustedProxyCidrs requires either that external acknowledgement or this chart's
    # global+API policy to be enabled with non-empty peers.
    externalPolicyAcknowledged: false

# nginx Ingresses for externally-routed services. Templated from the live cluster state.
# The prometheus ingress is intentionally excluded (owned by the prometheus/GMP app).
ingress:
  enabled: true
  className: "nginx"
  tlsSecretName: "ingress-tls"
  # Source-IP allowlists are infra-specific (Cloudflare ranges, GCP NAT egress, ops IPs) and are
  # set per-environment in the overlay values. Left empty here so the whitelist annotation is
  # omitted entirely when unset (no allowlist == nginx allows all).
  whitelistSourceRange: ""
  # Client-IP source for every ingress that routes directly to the API. The safe default ignores
  # request headers and forwards nginx's direct peer address. Enable this only when every such
  # request arrives through Cloudflare; rendering then requires a trusted source whitelist.
  # This map supports only `enabled`, which must be an unquoted YAML boolean.
  cloudflareClientIp:
    enabled: false
  # socket-servers uses its own list (the other ingresses add a trailing ops IP); set in overlay.
  socketWhitelistSourceRange: ""

# URL of the Prometheus service used for subscription quota queries (api/invocation/util.py).
# The service name varies by deployment — override in environment values.
prometheusUrl: "http://prometheus"

# S3/object-storage credentials secret name (key: access-key-id, secret-access-key, endpoint-url, aws-region, bucket)
s3SecretName: "s3-credentials"

# External managed Postgres (chutes-db secret) for DB_RW_URL / DB_RO_URL
chuteDb:
  enabled: false
  secretName: "chutes-db"

api:
  replicaCount: 1
  # Fixed ChuteFS token-key activation quorum for this stable API serving cohort.
  # Production overlays must set this to the intended minimum serving replica target.
  chutefsTokenRequiredAckCount: 1
  # Fixed Registration V2 recovery-key activation quorum for the stable API cohort.
  # Production overlays must set this to the intended minimum serving replica target.
  gpuRegistrationRecoveryRequiredAckCount: 1
  # Preserve the immutable v1 GPU runtime-session contract during API/miner/agent
  # rollout. Enable only after every durable miner consumer accepts v2.
  gpuRuntimeSessionV2Enabled: false
  image: validator:latest
  imagePullPolicy: Always
  limitConcurrency: 1024
  timeoutGracefulShutdown: 120
  terminationGracePeriodSeconds: 180
  dbPoolSize: 24
  dbOverflow: 8
  rateLimitCount: 10
  openclGravalUrl: "https://new-graval.chutes.ai:8443"
  rollingUpdate:
    maxSurge: "25%"
    maxUnavailable: "25%"
  livenessProbe:
    initialDelaySeconds: 40
    periodSeconds: 15
    timeoutSeconds: 7
    successThreshold: 1
    failureThreshold: 4
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  resources:
    requests:
      cpu: "2"
      memory: "8Gi"
      ephemeral-storage: "2Gi"
    limits:
      cpu: "2"
      memory: "8Gi"
      ephemeral-storage: "2Gi"
  service:
    type: ClusterIP
    port: 8000
    targetPort: 8000
  nodeSelector: {}
  hpa:
    enabled: false
    minReplicas: 2
    maxReplicas: 30
    targetCPUUtilizationPercentage: 70

  # KEDA ScaledObject — when enabled, replaces the CPU-based HPA above (api.hpa is skipped).
  # Requires KEDA to be installed in the cluster. Defaults mirror the live prod `api-scaler`
  # so enabling it is a no-op; override per-environment as needed.
  keda:
    enabled: false
    name: api-scaler          # must match the live ScaledObject name to adopt it (KEDA names its HPA keda-hpa-<name>)
    minReplicas: 50
    maxReplicas: 200
    cooldownPeriod: 120
    pollingInterval: 15
    # HPA scaling behaviour (advanced.horizontalPodAutoscalerConfig.behavior)
    behavior:
      scaleDown:
        stabilizationWindowSeconds: 300
        periodSeconds: 120
        pods: 2
      scaleUp:
        stabilizationWindowSeconds: 30
        periodSeconds: 60
        pods: 3
    # Resource triggers (Utilization %). Set enabled: false to drop a trigger.
    cpu:
      enabled: true
      utilization: 65
    memory:
      enabled: true
      utilization: 75
    # Prometheus in-flight-requests trigger. serverAddress/query default in-template to the
    # in-cluster prometheus and the live prod query (namespace-substituted); override here if needed.
    inflight:
      enabled: true
      serverAddress: ""
      query: ""
      threshold: "200"

  affinity:
    nodeAffinity: {}
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - api
          topologyKey: kubernetes.io/hostname
  tolerations: []

socket:
  image: validator:latest
  imagePullPolicy: Always
  replicaCount: 1
  livenessProbe:
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  resources:
    requests:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 8000
    targetPort: 8000
  nodeSelector: {}
  hpa:
    enabled: false
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70

  affinity:
    nodeAffinity: {}
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - socket
          topologyKey: kubernetes.io/hostname
  tolerations: []

eventSocket:
  image: validator:latest
  imagePullPolicy: Always
  replicaCount: 1
  livenessProbe:
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  resources:
    requests:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 8000
    targetPort: 8000
  nodeSelector: {}
  hpa:
    enabled: false
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70

  affinity:
    nodeAffinity: {}
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - event-socket
          topologyKey: kubernetes.io/hostname
  tolerations: []

forge:
  replicaCount: 1
  image: forge:latest
  imagePullPolicy: Always
  busyboxImage: busybox:1.36
  depot:
    enabled: false
  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "1Gi"
      ephemeral-storage: "1Gi"
  nodeSelector: {}
  hpa:
    enabled: false
    minReplicas: 1
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70

  affinity:
    nodeAffinity: {}
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - forge
          topologyKey: kubernetes.io/hostname
  tolerations: []
  volumes: []
  volumeMounts: []

remoteForge:
  enabled: false
  replicaCount: 4
  image: parachutes/remote-forge:latest
  imagePullPolicy: Always
  resources:
    requests:
      cpu: "750m"
      memory: "2Gi"
    limits:
      cpu: "750m"
      memory: "2Gi"
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - remote-forge
          topologyKey: kubernetes.io/hostname
  tolerations: []

gravalWorker:
  replicaCount: 1
  image: validator:latest
  imagePullPolicy: Always
  maxAsyncTasks: 1
  livenessProbe:
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  # fs-hash blob cache cap (blobs are ~70-270MB each; 3Gi holds many). The
  # in-process LRU prune keeps the cache under this; the ephemeral-storage limit
  # below is sized well above it so the cache has room without evicting the pod.
  fsCacheMaxSize: "3Gi"
  # NOTE: ephemeral-storage MUST be set explicitly. On GKE Autopilot an unset value
  # is defaulted to 1Gi, which the fs-hash blob cache overruns and gets the pod
  # evicted. If a deploy-time overlay (e.g. chutes-ops values) overrides this
  # resources block, set ephemeral-storage there too — it is not inherited.
  resources:
    requests:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "6Gi"
    limits:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "6Gi"
  nodeSelector: {}
  hpa:
    enabled: false
    minReplicas: 1
    maxReplicas: 20
    targetCPUUtilizationPercentage: 70

  affinity:
    nodeAffinity: {}
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - graval-worker
          topologyKey: kubernetes.io/hostname
  tolerations: []

watchtower:
  enabled: true
  replicaCount: 1
  image: validator:latest
  imagePullPolicy: Always
  resources:
    requests:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "1"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

cacher:
  image: validator:latest
  imagePullPolicy: Always
  schedule: "*/10 * * * *"
  suspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

chuteAutoscaler:
  image: validator:latest
  imagePullPolicy: Always
  schedule: "1,16,31,46 * * * *"
  suspend: false
  dryrunSchedule: "*/2 * * * *"
  dryrunSuspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

paymentWatcher:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  replicaCount: 1
  subtensor: "ws://subtensor:9944"
  livenessProbe:
    initialDelaySeconds: 30
    periodSeconds: 30
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 15
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 8000
    targetPort: 8000
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
  tolerations: []

btTxTracker:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  replicaCount: 0
  subtensor: "ws://subtensor:9944"
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 8000
    targetPort: 8000
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
  tolerations: []

usageTracker:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  replicaCount: 1
  livenessProbe:
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  readinessProbe:
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    successThreshold: 1
    failureThreshold: 3
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  nodeSelector: {}
  affinity: {}
  tolerations: []

metasync:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "*/3 * * * *"
  suspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
  subtensor: "wss://entrypoint-finney.opentensor.ai"
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
  tolerations: []

weightsetter:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  resources:
    requests:
      cpu: "500m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
  subtensor: "wss://entrypoint-finney.opentensor.ai"
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
  tolerations: []

auditExporter:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "1 * * * *"
  suspend: false
  # Keep audit Jobs/pods longer than the global default for after-the-fact inspection.
  ttlSecondsAfterFinished: 21600
  subtensor: "wss://entrypoint-finney.opentensor.ai"
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  nodeSelector: {}

failedChuteCleanup:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "*/15 * * * *"
  suspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  nodeSelector: {}

balanceRefresher:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "* * * * *"
  suspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

logProber:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "*/3 * * * *"
  suspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

serverHealthProber:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "*/5 * * * *"
  suspend: false
  resources:
    requests:
      cpu: "250m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "250m"
      memory: "512Mi"
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

connProber:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "*/3 * * * *"
  suspend: false
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
  nodeSelector: {}

# Managed Redis connection (credentials from redis-secret)
redis:
  host: ""
  port: 6379
  # Name of a secret with a 'ca' key containing the TLS CA certificate (optional)
  cacertSecret: ""
  # File mode for the mounted CA cert, as an OCTAL integer (write the leading zero!).
  # 0444 = r--r--r-- (read-only): least-privilege-correct, since a mounted CA cert is only
  # ever read, never written (the owner-write bit in the k8s default 0644 is pure noise).
  # Non-functional regardless (secret volumes mount read-only). Renders to decimal 292.
  cacertMode: 0444

cmRedis:
  enabled: true
  shard_count: 1
  image: redis:7
  imagePullPolicy: IfNotPresent
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
  tolerations: []
  resources:
    requests:
      cpu: 1
      memory: 2Gi
    limits:
      cpu: 1
      memory: 2Gi
  service:
    type: ClusterIP
    port: 6379
  config:
    customArgs: []

quotaRedis:
  enabled: true
  image: redis:7
  imagePullPolicy: IfNotPresent
  replicaCount: 1
  nodeSelector: {}
  affinity: {}
  tolerations: []
  resources:
    requests:
      cpu: 2
      memory: 8Gi
    limits:
      cpu: 2
      memory: 8Gi
  service:
    type: ClusterIP
    port: 6379
  config:
    customArgs: []

# Dedicated single-instance Loki for the chute log shipper (pre-registration pod logs). This is
# NOT the ops monitoring cluster: it is platform-owned, in-namespace, bounded to 24h retention, and
# read by the API (owner/miner CLI) + the ops Grafana (as a datasource, over the internal network).
# The API pushes lines directly to it (never via stdout). Not client-reachable (NetworkPolicy).
loki:
  enabled: true
  image: grafana/loki:3.7.4
  imagePullPolicy: IfNotPresent
  nodeSelector: {}
  affinity: {}
  tolerations: []
  resources:
    requests:
      cpu: 1
      memory: 2Gi
    limits:
      cpu: 2
      memory: 4Gi
  service:
    type: ClusterIP
    port: 3100
  # Bounded local storage; 24h retention keeps the footprint small.
  persistence:
    enabled: true
    size: 20Gi
    storageClassName: ""
  retentionHours: 24
  # NetworkPolicy: Loki is default-deny and is NOT client-reachable. In-namespace clients (the API)
  # opt in via the `loki-access: "true"` pod label — that stays locked to the workloads this chart
  # deploys. Any OTHER peer that may reach Loki (e.g. the ops Grafana namespace) is cluster-specific
  # policy: set it here as a list of raw NetworkPolicy ingress `from` peers. The chart names no
  # external infra and defaults to none; the ops repo, which manages these values and has the
  # cluster-wide context, sets who/what may read Loki. Example (in the ops repo overlay):
  #   extraIngressFrom:
  #     - namespaceSelector:
  #         matchLabels:
  #           kubernetes.io/metadata.name: monitoring
  extraIngressFrom: []

registry:
  enabled: true
  image: registry:3.0.0-rc.3
  imagePullPolicy: IfNotPresent
  replicaCount: 2
  nodeSelector: {}
  affinity:
    nodeAffinity: {}
  tolerations: []
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 512Mi
  service:
    type: ClusterIP
    port: 5000
  config:
    redirectDisable: false
    type: "gcs"

registryProxy:
  image: nginx:1.31
  imagePullPolicy: IfNotPresent
  busyboxImage: busybox:1.36
  # Public *.chutes.ai cert (GoDaddy). When registry.chutes.ai points straight at this
  # Service's L4 LB, nginx faces the client directly, so the cert must be publicly trusted and
  # cover the host. registry-proxy-tls is a Cloudflare Origin cert (not publicly trusted) and
  # must NOT be used on the client-facing path.
  tlsSecretName: "chutes-tls"
  # Optional shared secret proving a /registry/auth request arrived via this proxy (defense in
  # depth: without it, anyone able to reach the API's /registry/auth off-proxy could spoof
  # X-Real-IP to force the legacy branch and bypass mTLS). When name is set, the proxy injects
  # it as X-Registry-Proxy-Auth and the API enforces it; leave name empty to disable. The
  # secret is created out of band (like attestation-proxy-auth) and its value must be sed-safe
  # (hex / base64url -- no '|' or '&').
  proxyAuthSecret:
    name: ""
    key: "secret"
  config:
    workerProcesses: 64
    workerConnections: 4096
    # tokenRealm: override the Www-Authenticate Bearer realm URL (default: https://registry.chutes.ai/v2/_token)
  replicaCount: 1
  nodeSelector: {}
  hpa:
    enabled: false
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70

  affinity:
    nodeAffinity: {}
  tolerations: []
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
      ephemeral-storage: "1Gi"
    limits:
      cpu: 500m
      memory: 512Mi
      ephemeral-storage: "1Gi"
  service:
    type: LoadBalancer
    port: 443
    annotations: {}
    # Local preserves the real VM source IP on the GCP L4 LB (needed for registry auth's
    # IP-based VM resolution). Only override to Cluster if IP-based resolution is not used.
    externalTrafficPolicy: Local
    # Reserved static IP to pin the LB across reconciles (set in the overlay once the address
    # is reserved in GCP). Empty = let GKE assign an ephemeral IP.
    loadBalancerIP: ""

cpuScheduler:
  enabled: true
  replicaCount: 1
  image: validator:latest
  imagePullPolicy: Always
  nodeSelector: {}
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/name
                  operator: In
                  values:
                    - cpu-scheduler
            topologyKey: kubernetes.io/hostname
  tolerations: []
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"

gpuPlatformScheduler:
  enabled: true
  replicaCount: 1
  image: validator:latest
  imagePullPolicy: Always
  nodeSelector: {}
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
                - key: app.kubernetes.io/name
                  operator: In
                  values:
                    - gpu-platform-scheduler
            topologyKey: kubernetes.io/hostname
  tolerations: []
  readinessProbe:
    initialDelaySeconds: 2
    periodSeconds: 2
    timeoutSeconds: 5
    failureThreshold: 1
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
    limits:
      cpu: "500m"
      memory: "2Gi"

# autostaker runs as a CronJob (one-shot api/autostaker.py per tick), not a long-running
# worker. validatorSs58 left empty here -> cronjobEnv falls back to the validator-credentials
# secret; set a literal in the overlay to match prod.
autostaker:
  enabled: true
  image: validator:latest
  imagePullPolicy: Always
  schedule: "* * * * *"
  suspend: false
  # Retain more staking Job history than the global default.
  successfulJobsHistoryLimit: 15
  failedJobsHistoryLimit: 15
  ttlSecondsAfterFinished: 21600
  subtensor: "wss://entrypoint-finney.opentensor.ai"
  validatorSs58: ""
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
      ephemeral-storage: "1Gi"
    limits:
      ephemeral-storage: "1Gi"
  affinity: {}
  tolerations: []

# New services (disabled by default -- enable in your overlay values)
attestationProxy:
  enabled: false
  replicaCount: 2
  image: nginx:1.31
  imagePullPolicy: IfNotPresent
  busyboxImage: busybox:1.36
  tlsSecretName: "chutes-tls"
  # Secret the render init container and the API both read (must be the SAME value, so the
  # injected X-Attestation-Proxy-Auth matches what the API enforces). Wired optional so the pod
  # starts if unprovisioned; the API guard then fails closed (503) rather than bypassing.
  proxyAuthSecret:
    name: "attestation-proxy-auth"
    key: "secret"
  serverName: "tdx-attestation.chutes.ai"
  # Placeholder source ranges (RFC5737 docs net). Set the real trusted real-IP sources in the
  # overlay -- these are environment-specific load-balancer/NAT addresses.
  realIpFrom:
    - "192.0.2.1"
    - "192.0.2.2"
    - "192.0.2.3"
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: "1Gi"
    limits:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: "1Gi"
  service:
    type: LoadBalancer
    # Reserved static LB IP (empty = GCP auto-assigns). Pin in the overlay so the IP survives a
    # Service recreate and tdx-attestation.chutes.ai keeps resolving.
    loadBalancerIP: ""
    port: 8443
    annotations: {}
  tolerations: []
  affinity: {}

# cvm-chutes-proxy: single mTLS front for ALL CVM attestation calls on cvm.chutes.ai. Deployed
# alongside the attestation-proxy LB; fronts all six /servers attestation endpoints and injects
# X-Attestation-Proxy-Auth (the API's require_attestation_proxy fails closed without it). Enable in the
# overlay once cvm.chutes.ai DNS + the LB exist. Assumes PROXY protocol in front (matches
# attestation-proxy); set realIpFrom to the real trusted sources.
cvmProxy:
  enabled: false
  replicaCount: 2
  image: nginx:1.31
  imagePullPolicy: IfNotPresent
  busyboxImage: busybox:1.36
  tlsSecretName: "chutes-tls"
  serverName: "cvm.chutes.ai"
  # Secret the render init container and the API both read (must be the SAME value, so the
  # injected X-Cvm-Proxy-Auth matches what the API enforces). Wired optional so the pod starts if
  # unprovisioned; the API guard then fails closed (503) rather than bypassing.
  proxyAuthSecret:
    name: "cvm-proxy-auth"
    key: "secret"
  # Max request body for the chute-log-shipper ingest location (bounds per-request memory, since
  # FastAPI buffers the whole JSON body before the app's line cap applies). ~8x the guest's default
  # 1 MiB batch; if a batch exceeds it, nginx 413s and the guest splits + retries smaller. Only the
  # logs location is capped; attestation stays unbounded (see cvm-proxy-cm.yaml).
  logsMaxBodySize: "8m"
  # No realIpFrom: cvm.chutes.ai routes straight to this LB (no PROXY-protocol intermediary),
  # so the client IP is $remote_addr directly (preserved via the Service's
  # externalTrafficPolicy: Local). Contrast attestationProxy, which sits behind the VM-side
  # nginx L4 stream and therefore needs set_real_ip_from + proxy_protocol.
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: "1Gi"
    limits:
      cpu: 250m
      memory: 512Mi
      ephemeral-storage: "1Gi"
  service:
    type: LoadBalancer
    # Reserved static LB IP that cvm.chutes.ai resolves to (empty = GCP auto-assigns). Pin in
    # the overlay.
    loadBalancerIP: ""
    # Public HTTPS on 443 (cvm.chutes.ai connects directly, no :port suffix); LB forwards to
    # the pod's unprivileged targetPort 8443 (nginx runs non-root).
    port: 443
    annotations: {}
  tolerations: []
  affinity: {}

claudeProxy:
  enabled: false
  replicaCount: 2
  image: claude-proxy:latest
  imagePullPolicy: Always
  backendUrl: "http://api.chutes.svc.cluster.local:8000/v1/chat/completions"
  backendHostHeader: "llm.chutes.ai"
  caddyDomain: "claude.chutes.ai"
  resources:
    requests:
      cpu: 750m
      memory: 2Gi
      ephemeral-storage: "1Gi"
    limits:
      cpu: 750m
      memory: 2Gi
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 8181
  tolerations: []
  affinity: {}

pgRouter:
  enabled: false
  replicaCount: 3
  haproxyImage: haproxy:3.3.10
  healthCheckImage: validator:latest
  imagePullPolicy: IfNotPresent
  credentialsSecret: "pg-router-credentials"
  # Placeholder backend addresses (RFC5737 docs net). Real Postgres endpoints are
  # environment-specific -- set pgRouter.backends in the overlay.
  backends:
    - name: dallas
      address: "192.0.2.10:5432"
      healthPort: 8008
      backup: false
    - name: iowa
      address: "192.0.2.11:5432"
      healthPort: 8009
      backup: true
  resources:
    requests:
      cpu: "1"
      memory: 2Gi
      ephemeral-storage: "1Gi"
    limits:
      cpu: "1"
      memory: 2Gi
      ephemeral-storage: "1Gi"
  healthCheckResources:
    requests:
      cpu: 100m
      memory: 128Mi
      ephemeral-storage: "1Gi"
    limits:
      cpu: 100m
      memory: 128Mi
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 5432
  tolerations: []
  affinity: {}

responsesProxy:
  enabled: false
  replicaCount: 2
  image: responses-proxy:latest
  imagePullPolicy: Always
  backendUrl: "http://api.chutes.svc.cluster.local:8000/v1/chat/completions"
  backendHostHeader: "llm.chutes.ai"
  caddyDomain: "responses.chutes.ai"
  resources:
    requests:
      cpu: 750m
      memory: 2Gi
      ephemeral-storage: "1Gi"
    limits:
      cpu: 750m
      memory: 2Gi
      ephemeral-storage: "1Gi"
  service:
    type: ClusterIP
    port: 8181
  tolerations: []
  affinity: {}

gmp:
  # Set true when using GKE Managed Prometheus.
  # When enabled: PodMonitoring CRDs are created and prometheus.io/scrape
  # annotations are suppressed (GMP ignores them).
  # When disabled: standard prometheus.io/scrape annotations are rendered
  # instead for a self-managed Prometheus instance.
  enabled: false
  interval: "30s"

teeMeasurements:
    # # Strict nested format: each group contains shared image/TEE fields and a required non-empty
    # # hardware list. Each hardware item contains name, optional description, expected_gpus,
    # # gpu_count, and exactly rtmr0 (TDX) or measurement (SEV-SNP). Set rc: true for an
    # # attestable release candidate excluded from the public endpoint and minimum-version fallback.
    # measurements:
    #   - version: "1.3.0"
    #     tee_type: "tdx"
    #     debug: false
    #     rc: false
    #     mrtd: "PLACEHOLDER_MRTD_96_HEX_CHARS"
    #     rtmr1: "PLACEHOLDER_RTMR1_96_HEX_CHARS"
    #     rtmr2: "PLACEHOLDER_RTMR2_96_HEX_CHARS"
    #     runtime_rtmr3: "PLACEHOLDER_RUNTIME_RTMR3_96_HEX_CHARS"
    #     hardware:
    #       - name: "8xh200"
    #         rtmr0: "PLACEHOLDER_H200_RTMR0_96_HEX_CHARS"
    #         expected_gpus:
    #           - "h200"
    #         gpu_count: 8
  measurements: []
  revokedMeasurements: []
